# MongoDB Connection Configuration for Training
# Copy this file to .env in your project root and configure your MongoDB connection

# REQUIRED: MongoDB Connection String
# Choose ONE of the options below and uncomment it

# OPTION 1: MongoDB Atlas (Recommended for Training)
# Sign up at https://cloud.mongodb.com/ for free tier
# Replace username, password, and cluster details with your actual credentials
# MONGO_URI=mongodb+srv://username:password@cluster0.abc123.mongodb.net/?retryWrites=true&w=majority

# OPTION 2: Local MongoDB
# For local MongoDB installation
# MONGO_URI=mongodb://localhost:27017/

# OPTION 3: Docker MongoDB
# If using Docker: docker run -d -p 27017:27017 mongo:latest
# MONGO_URI=mongodb://localhost:27017/

# OPTION 4: Instructor-Provided Cluster
# Ask your instructor for shared training cluster credentials
# MONGO_URI=mongodb+srv://training_user:training_pass@training-cluster.mongodb.net/

# OPTIONAL: Database Configuration
# Uncomment and modify if you want to change defaults

# Database name (default: hotel_management)
# DB_NAME=my_training_database

# Collection prefix (default: none)
# COLLECTION_PREFIX=student_

# IMPORTANT NOTES:
# 1. Remove the # symbol to uncomment a line
# 2. Use only ONE MONGO_URI option
# 3. Keep your credentials secure - never commit real passwords to public repositories
# 4. For MongoDB Atlas, make sure to whitelist your IP address (or use 0.0.0.0/0 for training)

# Example of a complete configuration:
# MONGO_URI=mongodb+srv://john_doe:mySecretPassword123@cluster0.abc123.mongodb.net/?retryWrites=true&w=majority
# DB_NAME=hotel_analytics_training